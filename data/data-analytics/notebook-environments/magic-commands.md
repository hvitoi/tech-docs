# Magic Commands

- Execute bash commands in a notebook cell

```python
%pip install slack_sdk
from slack_sdk import WebClient
from slack_sdk.errors import SlackApiError
```

````python
# same with vanilla python
import subprocess
subprocess.run(["pip", "install", "slack_sdk"])
```
