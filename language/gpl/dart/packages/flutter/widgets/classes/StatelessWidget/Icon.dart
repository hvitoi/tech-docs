import 'package:flutter/material.dart';

Widget main() {
  return Icon(
    Icons.favorite,
    color: Colors.pink,
    size: 24.0,
    semanticLabel: 'Text to announce in accessibility modes',
  );
}
