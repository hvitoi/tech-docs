import 'package:flutter/foundation.dart';

/**
 * Prints a message to the console, which you can access using 
 * the "flutter" tool's "logs" command ("flutter logs").
 */
void main() {
  debugPrint("blabla");
}
