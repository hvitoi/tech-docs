const arr = [1, 2, 3, 4, 5];
const phrase = "Once upon a time";

// Iterates array
for (let el of arr) {
  console.log(el);
}

// Iterates a string
for (let letter of phrase) {
  console.log(letter);
}

// Obs: Objects are not interable in forof!!
