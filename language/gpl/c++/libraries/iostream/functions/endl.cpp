#include <iostream>

using namespace std;

int main() {
  // A manipulator that inserts a newline character (\n) into the output and
  // flushes the output buffer.
  cout << "Hello World" << endl;

  return 0;
}
