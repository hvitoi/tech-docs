// + (addition)
// - (subtraction)
// * (multiplication)
// / (division)
// % (modulus)

#include <stdio.h>

int main() {

  int a = 1 + 1;
  int b = 2 - 1;
  int c = 2 * 2;
  int d = 4 / 2;
  float e = 5 / (float)2;
  int f = 5 % 2;

  float temp = (temp * 9 / 5) + 32;

  return 0;
}
