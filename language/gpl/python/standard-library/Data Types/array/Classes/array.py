# %%
from array import array

# it's a raw primitive array! Contains only
# rarely used in production code. "list" built-in container is preferred

# 'i' for integer typ
arr = array("i", [1, 2, 3, 4])
