# unittest

```shell
python -m unittest test_main                          # all
python -m unittest test_main.TestStrategies           # one class
python -m unittest test_main.TestGet.test_get_raises_on_empty_pool  # one test
python -m unittest discover -v                        # auto-discover

python test_main.py -v # or run the file directly
```
