import venv

# "venv" is the native native to create python environments
# other alternatives are "virtualenv" and "pipenv", which are external modules

## Creates a new virtual environment (a folder with the necessary files)
# python -m venv /path/to/my-new-environment
# python -m venv .venv # it's usually named so

## Chroot the environment
# source .venv/bin/activate
# echo $VIRTUAL_ENV
