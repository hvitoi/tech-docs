{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run File",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/build/${fileBasenameNoExtension}.out",
      "preLaunchTask": "build",
      "postDebugTask": "remove build folder"
    }
  ]
}
