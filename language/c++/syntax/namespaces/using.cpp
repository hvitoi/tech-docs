#include <iostream>
using namespace std;

// allows you to use the components without needing to prefix them with "std::",
// e.g., "std::cout"
// in large projects can lead to naming conflicts, so it’s better to use std::
// explicitly in professional code.

int main() {
  cout << "Hello World" << endl;

  return 0;
}
