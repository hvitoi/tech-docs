# It's a context-sensitive keyword (sometimes called a soft keyword or pseudo-keyword) used only in type hinting contexts.
# "type" used as a type alias declaration is not a keyword in the traditional Python grammar sense
# %%

type SetOfIntegers = set[int]
