// O(1)
// "Pop" removes an element from the end of an array
const arr = [];

arr.push(1); // returns the index (0)
arr.push(2); // returns the index (1)
const element = arr.pop(); // get number 2

console.log(element);
console.log(arr);
