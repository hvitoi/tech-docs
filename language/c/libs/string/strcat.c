#include <stdio.h>
#include <string.h>

int main() {
  char s1[] = "abcd";
  char s2[] = "efgh";

  // appends str1 & str2
  strcat(s1, s2);

  // strlwr(s1);
  // strupr(s1);
  // strset(s1, '?');
  // strnset(s1, '?', 1);
  // strrev(s1);

  return 0;
}
