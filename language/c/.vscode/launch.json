{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run File",
      "type": "lldb",
      "request": "launch",
      "preLaunchTask": "build",
      "program": "${fileDirname}/${fileBasenameNoExtension}.out"
    }
  ]
}
