# Globbing Patterns
# - Also known as `standard wildcards`
## Special characters

# - `*` for any char
# - `?` for single char
# - `[]` for range
# - `{}` for range
# - `!` for negation
# - `\` for escape
# - `/` for directory
# - `#` for comment

touch abc{1..9}
