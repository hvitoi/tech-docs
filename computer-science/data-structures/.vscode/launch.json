{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run C++",
      "type": "lldb",
      "request": "launch",
      "preLaunchTask": "clang build",
      "postDebugTask": "remove build folder",
      "program": "${fileDirname}/build/${fileBasenameNoExtension}.out",
      "console": "internalConsole"
    }
  ]
}
