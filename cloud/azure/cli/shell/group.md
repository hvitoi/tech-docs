# Resource Group

```shell
# List
az group list

# Create
az group create --name "demo-rg" --location "southcentralus"

# Update
az group update --name "demo-rg" --set...

# Delete
az group delete --name `demo-rg` --no-wait --yes
```
