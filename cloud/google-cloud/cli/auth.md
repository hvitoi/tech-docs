# gcloud auth

## login

```shell
# Login to GCloud SDK
gcloud auth login
```

## application-default

```shell
# Authenticate (writes application default credentials)
gcloud auth application-default login
```
