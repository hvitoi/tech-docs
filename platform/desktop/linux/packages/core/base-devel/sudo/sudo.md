# sudo

- SUDO - Super User DO

```shell
# Change into super user
sudo -s # Same as su -

# print sudoers configuration
sudo -ll

# login as root
sudo su

# login as root
sudo -u <user> -- sh -c "cd /; /bin/bash"
```
