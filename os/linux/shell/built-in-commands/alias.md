# alias

```sh
# Create alias
alias ll = "ls -laF"
alias pl = "pws; ls"
alias tell = "whoami; hostname; pwd"
alias dir = "ls -l | grep ^d"

# List all aliases
alias

# Remove alias
unalias `alias_name`
```
