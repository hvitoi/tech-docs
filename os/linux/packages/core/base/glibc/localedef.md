# localedef

```shell
# list the locales available to be used at /etc/locale.conf
# these are stored at /usr/lib/locale/locale-archive
# and generated by locale-gen (based on /etc/locale.gen)
localedef --list-archive
localectl list-locales # alternative
```
