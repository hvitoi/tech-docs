# mkdir

```shell
# Create a directory
mkdir "dirname"

# Create multiple dirs
mkdir "dir1" "dir2" "dir3"
mkdir "/mmt/{boot,home}"

# Create parents directories as needed
mkdir -p "/home/ubuntu/.ssh"
```
