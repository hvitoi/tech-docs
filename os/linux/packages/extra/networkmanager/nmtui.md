# nmtui

- Network Manager Text User Interface
- Options
  - `Edit a connection`
  - `Activate a connection`
  - `Set system hostname`

```shell
nmtui
```

- It's possible to modify `teams` and `bonds`
