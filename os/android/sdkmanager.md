# sdkmanager

```sh
# Update the CLI
sdkmanager --update --no_https

# Install tools
sdkmanager \
  "emulator" \
  "build-tools;30.0.2" \
  "platforms;android-30" \
  "system-images;android-30;google_apis_playstore;x86_64"
```
